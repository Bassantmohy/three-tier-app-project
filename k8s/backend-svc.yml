apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend-deployment
  name: backend-deployment
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: backend-deployment
  type: ClusterIP
status:
  loadBalancer: {}
